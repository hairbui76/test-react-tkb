{
	"name": "react",
	"version": "1.0.0",
	"description": "",
	"main": "server.js",
	"scripts": {
		"start": "set NODE_OPTIONS=--openssl-legacy-provider && netlify-lambda serve src",
		"build": "netlify-lambda build src && cd client && npm i && npm run build",
		"build-client": "cd client && npm run build",
		"client": "cd client && npm run dev",
		"dev": "concurrently -n 'server,client' -c 'red,green' \"nodemon server.js\" \"npm run client\""
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"@fortawesome/fontawesome-free": "^6.2.1",
		"concurrently": "^7.6.0",
		"dotenv": "^16.0.3",
		"express": "^4.18.2",
		"html2canvas": "^1.4.1",
		"mongoose": "^6.7.5",
		"netlify-lambda": "^2.0.15",
		"serverless-http": "^3.1.0",
		"use-undo": "^1.1.1"
	},
	"devDependencies": {
		"nodemon": "^2.0.20"
	}
}